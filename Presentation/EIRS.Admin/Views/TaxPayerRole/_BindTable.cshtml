@model IList<EIRS.BOL.usp_GetTaxPayerRoleList_Result>
@using EIRS.Common

@if (Model != null && Model.Count > 0)
{
    foreach (var item in Model)
    {
        <tr>
            <td>@item.AssetTypeName</td>
            <td>@item.TaxPayerTypeName</td>
            <td>@item.TaxPayerRoleName</td>
            <td>@item.IsMultiLinkableText</td>
            <td>@item.ActiveText</td>
            <td>
                <div class="btn-group">
                    <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown" aria-expanded="false">
                        Actions
                        <span class="caret"></span>
                    </button>
                    <ul class="dropdown-menu" role="menu">
                        <li>
                            <a href="@Url.Action("Details","TaxPayerRole", new { id = item.TaxPayerRoleID, name=item.TaxPayerRoleName.ToSeoUrl()})">
                                View Details
                            </a>
                        </li>
                        <li>
                            <a href="@Url.Action("Edit", "TaxPayerRole", new { id = item.TaxPayerRoleID, name=item.TaxPayerRoleName.ToSeoUrl() })">
                                Edit Details
                            </a>
                        </li>
                        <li>
                            <a onclick="javascript:jsfn_ChangeStatus(@item.TaxPayerRoleID)">
                                Mark @(item.Active.GetValueOrDefault() ? "Inactive" : "Active")
                            </a>
                        </li>
                    </ul>
                </div>
            </td>
        </tr>
    }
}
