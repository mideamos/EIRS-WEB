@model IList<EIRS.BOL.usp_GetIndividualList_Result>
@using EIRS.Common
@{
    Layout = "~/Views/Shared/_TaxPayerLayout.cshtml";
    ViewBag.Title = "Individual Tax Payer";
}

<div class="title">
    <h1>Individual Tax Payer</h1>
    <hr />
</div>

<div class="portlet light">
    <div class="portlet-title">
        <div class="caption">
            List of Individual Tax Payers
        </div>
        <div class="actions">

        </div>
    </div>
    <div class="portlet-body">
        <table class="dt-responsive table table-bordered v-middle" width="100%" id="tblIndividual">
            <thead class="red-th">
                <tr>
                    <th>RIN</th>
                    <th>Individual Name</th>
                    <th>TIN</th>
                    <th class="desktop">Address</th>
                    <th class="action-th all">Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.IndividualRIN</td>
                        <td>@item.FirstName</td>
                        <td>@item.TIN</td>
                        <td>@item.ContactAddress</td>
                        <td>
                            <div class="btn-group">
                                <button type="button" class="btn btn-theme dropdown-toggle md-skip btn-xs" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    Action<span class="caret"></span>
                                </button><ul class="dropdown-menu">
                                    <li>
                                        <a onclick="javascript:jsfn_TaxPayerIndividualInformation(@item.IndividualID)">
                                            Quick View
                                        </a>
                                    </li>
                                    <li>
                                        <a href="@Url.Action("IndividualDetails","TaxPayerPanel")">
                                            Tax Payer Details
                                        </a>
                                    </li>
                                </ul>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@{Html.RenderPartial("_IndividualDetailPopup"); }

@section Footer {
    <script type="text/javascript" src="~/Scripts/jsTaxPayerIndividualList.js"></script>
}
