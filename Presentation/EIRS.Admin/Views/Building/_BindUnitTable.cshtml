@model IList<EIRS.BOL.usp_GetBuildingUnitNumberList_Result>
@using EIRS.Common

@if (Model != null && Model.Count > 0)
{
    foreach (var item in Model)
    {
        <tr>
            <td>@item.UnitNumber</td>
            <td>@item.UnitSize</td>
            <td>
                <div class="btn-group">
                    <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown" aria-expanded="false">
                        Actions
                        <span class="caret"></span>
                    </button>
                    <ul class="dropdown-menu" role="menu">
                        <li>
                            <a onclick="javascript:jsfn_ShowDetails(@item.BBUID)">
                                View Details
                            </a>
                        </li>
                        <li>
                            <a onclick="javascript:jsfn_RemoveLink(@item.BBUID,@item.BuildingID)">
                                Remove Unit
                            </a>
                        </li>
                    </ul>
                </div>
            </td>
        </tr>
    }
}



