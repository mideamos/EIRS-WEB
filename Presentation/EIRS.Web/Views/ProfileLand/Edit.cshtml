@model EIRS.Models.LandViewModel

@{
    ViewBag.Title = "Edit Land Asset";
}

<div class="title">
    <h1>
        Land Asset
    </h1>
    <hr>
</div>

<div class="portlet light">
    <div class="portlet-title">
        <div class="caption">
            Update Land
        </div>
        <div class="actions">
            <a href="@Request.UrlReferrer" class="btn btn-redtheme"> Cancel </a>
        </div>
    </div>
    <div class="portlet-body">
        @using (Html.BeginForm("Edit", "ProfileLand", FormMethod.Post, htmlAttributes: new { @id = "frmAddLand" }))
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true)
            @Html.HiddenFor(model => model.LandID, htmlAttributes: new { @value = Model.LandID })
            @Html.HiddenFor(model => model.LandRIN, htmlAttributes: new { @value = Model.LandRIN })

            if (ViewBag.Message != null)
            {
                <div class="alert alert-danger">
                    @ViewBag.Message
                </div>
            }

            Html.RenderPartial("_BindLandForm", Model);

            <div class="row">
                <div class="text-right col-sm-12">
                    <div class="form-group">
                        <input type="submit" class="btn-theme btn" value="Save" tabindex="20" />
                    </div>
                </div>
            </div>
        }
    </div>
</div>

@section Footer {
    <script type="text/javascript" src="~/Scripts/jsAddEditLand.js?v=@EIRS.Common.GlobalDefaultValues.VersionNumber"></script>
}
